defmodule SashaEdu.ChapterOne do
  @moduledoc """
  Пиши сюда решения
  """

  def hello() do
    "hello"
  end

  def hello_by_name(name) do
    "Hello, #{name}"
  end

  def hello_all(name1, name2) do
    "Hello, #{name1}, #{name2}"
  end

  def hello_by_manager(string) do
    "Hello, Manager #{string}"
  end

  def concat_string(word1, word2) do
    "#{word1}#{word2}"
  end
end
